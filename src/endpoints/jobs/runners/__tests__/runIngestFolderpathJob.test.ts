/**
 * - files are ingested
 *  - with a backend that supports folders
 *  - with a backend that does not support folders
 * - folders are ingested
 *  - jobs are created
 * - calls backend with right folerpath
 * - does nothing if backend is fimidara
 * - keeps continuation token and does not start from the beginning
 * - folderID
 * - folderpath
 *  - root folder
 */

describe('runIngestFolderpathJob', () => {
  test('files ingested with a backend that supports folders', async () => {});
});
