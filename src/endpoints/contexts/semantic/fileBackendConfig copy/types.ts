import {FileBackendConfig} from '../../../../definitions/fileBackend';
import {SemanticBaseProviderType} from '../types';

export interface SemanticFileBackendConfigProvider
  extends SemanticBaseProviderType<FileBackendConfig> {}
